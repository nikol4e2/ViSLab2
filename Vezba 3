 Параметри на Пуасоновата распределба
lambda <- 6  # Просечен број на повици на час

# Пресметка на веројатности
p0 <- dpois(0, lambda)  # Ниеден повик
p1 <- dpois(1, lambda)  # Еден повик
p2 <- dpois(2, lambda)  # Два повика
p3 <- dpois(3, lambda)  # Три повика
p4 <- dpois(4, lambda)  # Четири повика

# Резултати
probabilities <- c("Ниеден повик" = p0,
                   "Еден повик" = p1,
                   "Два повика" = p2,
                   "Три повика" = p3,
                   "Четири повика" = p4)

# Печатење на веројатностите
print(probabilities)
